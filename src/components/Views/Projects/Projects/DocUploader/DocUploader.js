import React from 'react';

const DocUploader = () => {
    return (
        <div style={{ display: "flex", justifyContent: "center", marginTop: "2%" }}>Coming Soon</div>
    );
}

export default DocUploader;